# The published design canvas: a ~2.5 MB generated bundle of the editor plus
# the artboards below. Re-seeded from the .dc.html files whenever it changes,
# so there is nothing to gain from versioning it.
bible-read-throughs.html
